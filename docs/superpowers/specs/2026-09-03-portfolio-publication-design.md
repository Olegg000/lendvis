# Публикация портфолио Лендвис — спека

Дата: 2026-09-03. Источник аудита: журналы потоков `~/Documents/claude/lendvis/01..06`, карта 78 репо, живая сверка 25 публичных репозиториев и 11 демо (все отдают 200 на 2026-09-03).

## 1. Решения владельца

| Вопрос | Решение |
|---|---|
| Бренд | **Lendvis / Лендвис**. Написание через `e` — как в репозиториях, логотипе и текстах сайта |
| Домен | **lendvis.ru**, регистрация в Timeweb |
| Аккаунт | Новая организация GitHub **`lendvis`** (свободна), витрина переезжает туда |
| Хостинг | VPS Timeweb + GitHub Pages, поддомены на одном домене |
| Почта | Новые ящики на домене, регистрация после eSIM |
| Логотип | `public/favicon.svg` — золотая «L» на чёрном. Утверждён, не меняется |

## 2. Адресная схема

Организация `lendvis`, хаб живёт в репозитории `lendvis/lendvis.github.io`, на него вешается `lendvis.ru`.
GitHub отдаёт проектные Pages той же организации на том же домене, поэтому все статические демо
получают адреса вида `lendvis.ru/<repo>/` без единой дополнительной настройки.

**Статика — GitHub Pages (апекс + www):**

```
lendvis.ru                          хаб
lendvis.ru/react-bugfix-demo/       разбор багов
lendvis.ru/python-faq-bot/          витрина бота записи
lendvis.ru/alice-smartlight/        умный дом
lendvis.ru/blockchain-practice/     мультичейн-практика
lendvis.ru/hotel-booking/           \
lendvis.ru/hr-assistant/             > страницы четырёх мобилок: кадры, описание, ссылка на APK
lendvis.ru/atelier/                 /
lendvis.ru/arena/
```

Первые четыре адреса — уже собранные Pages, переезжают вместе с репозиториями.
Четыре последних создаём: сейчас у мобилок нет ни страницы, ни релиза.
Работы без демо (`fabric-supply-chain-ts`, `waves-enterprise-integration`, `alice-skill-demo`,
`allTogether`, `volgaIT2025-flutter`) живут страницей на хабе и ссылкой на код — отдельный
адрес им не нужен.

**VPS Timeweb — поддомены под то, где нужен живой процесс:**

```
board.lendvis.ru    drawer: Node + Socket.IO — совместное рисование в реальном времени
quiz.lendvis.ru     quiz: Express + PostgreSQL — полноценная LMS с ролями
api.lendvis.ru      вебхук навыка Алисы: Яндексу нужен настоящий HTTPS-эндпоинт
mail                MX почты домена
```

Причина разделения не в экономии. Два демо сейчас показывают половину продукта:
у `drawer` на статике работает только локальный режим — то есть совместность, ради которой
проект и сделан, не видна; у `quiz` демо отвечает записанными фикстурами вместо живой базы.
VPS чинит ровно это. Остальные восемь статических сборок на VPS не выигрывают ничего,
а стоят двух десятков деплой-пайплайнов и собственного аптайма: если хост ляжет во время
визита заказчика, тёмным станет всё портфолио разом, а не одно демо.

DNS ведём в Timeweb: апекс — четыре A-записи GitHub Pages, поддомены — A на VPS, MX на почту.

Побочный выигрыш от собственного домена: у хаба уходит `/lendvis/` из base-пути и можно снять
HashRouter. Адреса `lendvis.ru/#/work` становятся `lendvis.ru/work` (на Pages — копией `index.html`
в `404.html`). Это одновременно вопрос вида ссылок и индексации.

## 3. Стоимость переезда, названная честно

GitHub переносит репозиторий вместе с редиректами, но **Pages редиректы не наследует**.
В момент переноса все живые адреса `olegg000.github.io/*` умирают безвозвратно.
Поэтому перенос делается **до** того, как адреса вошьются в двадцать пять README, OG-теги,
sitemap и подписи — иначе эта работа делается дважды.

Все публичные ссылки пишем сразу как `lendvis.ru/...`. Между переносом и покупкой домена
они несколько дней не отвечают — это допустимо: портфолио ещё нигде не анонсировано,
переходов по нему нет. Публикация и анонс — только после переключения DNS.

## 4. Витрина: что показываем

Сейчас на хабе восемь карточек плоским списком и ни одной страницы проекта.
Становится: тринадцать работ, семь кейсов и страница на каждую работу.

**Страница проекта** (`lendvis.ru/work/<slug>`): задача → решение → чем доказано →
живое демо во фрейме → кадры в рамке устройства → стек → ссылки на код и демо.
Витрина без страниц продаёт количество плиток; компании покупают одну развёрнутую историю решения.

### Открытый код — 13 работ

Публичных репозиториев осталось четырнадцать: хаб, `FlipCoin` (трогать нельзя, см. ниже)
и тринадцать витринных работ. Скрыто третьего сентября десять: `kmp-presentation`,
`ton-jetton-vesting`, `autoCheckMobileReact`, `autoCheckMobileFlutter`, `allTogether`,
`open-webui`, `hr-connect-mock-backend`, `vps-setup-script`, `screenplay-to-shotlist`,
`volgaIT2025-flutter`.

| Направление | Работы | Чем подтверждено |
|---|---|---|
| Сайты и веб-сервисы | `quiz`, `drawer`, `react-bugfix-demo` | в истории только владелец, заказчика в дереве нет |
| Мобильные приложения | `hotel-booking`, `android-hr-assistant`, `atelier-android`, `crossers-arena` | код переписан 2 сентября с нуля, дизайны авторские, соавторов нет |
| Блокчейн | `fabric-supply-chain-ts`, `waves-enterprise-integration`, `blockchain-practice` | написаны 25 июля под портфолио; `blockchain-practice` — обзорная страница направления |
| Бекенды и интеграции | `alice-301-backend` | собственный умный дом, личные данные лежат в приватном `alice-LOKAL` |
| Боты и мини-приложения | `python-faq-bot`, `alice-skill-demo` | написаны 25 июля под портфолио |
| Поддержка и багфиксы | `react-bugfix-demo` | четыре типичных бага в `main`, исправления и разбор в ветке `fix` |
| 1С | — | кода нет по природе направления, только кейс |
| Серверы и инфраструктура | **пусто** | `vps-setup-script` скрыт по решению владельца |
| Моделирование процессов | **пусто** | кандидат: DES-модель сортировочного центра |
| Тестирование и аудит | **пусто** | кандидат: обезличенная методика аудита |

**Следы соревновательных задач в мобилках вычищены 2026-09-03:** пакет `com.example.matule_new_main`
переименован в `com.example.hotelbooking` (61 файл, тема `Theme.Matulenewmain` → `Theme.HotelBooking`,
удалён алиас `Newtaskmatule2026uikitTheme`, сборка зелёная); в `atelier-android` адрес учебного стенда
`skill.matstart.ru/matule-api` заменён на нейтральный пример. В `android-hr-assistant` и
`crossers-arena` следов не было. Осталась косметика: у HR-ассистента `applicationId` до сих пор
`com.example.main3` — для витринного репозитория выглядит небрежно.

**Три направления из десяти теперь без единой открытой работы.** Либо закрываем их —
возвращаем `vps-setup-script`, оформляем модель СЦ и методику аудита, — либо убираем со страницы.

### Под NDA — 7 кейсов на сайте

На сайте сейчас семь: PrimeFlowers, питание по QR, спасение legacy-кода, игровой токен с вестингом,
автопроверка заданий, сценарий фильма в покадровый план, фотоальбом с картой для ОС Аврора.
Последние четыре написаны 3 сентября прямо в `data.ts` взамен снятого кода.
Ещё не вынесены на сайт тексты из `lendvis-out/cases/`: ProfiStart и 1С; файл `pgkfood.md`
перед публикацией обезличить — в нём осталось название заказчика.

### Дыра, которую надо закрыть

Сайт заявляет **десять направлений**, «за каждым — работа в проде, а не туториалы».
Под двумя направлениями публично нет ничего:

- **Моделирование процессов** — кандидат: DES-модель сортировочного центра (`~/Documents/claude/ozone-sc-sim`, SimPy/Salabim/Three.js). Оформить в репозиторий.
- **Тестирование и аудит** — кандидат: аудит PGK Food (13 находок с классификацией, 204 теста после hardening). Публикуем методику и обезличенный отчёт, без имени заказчика и без деталей уязвимостей.

Либо закрываем эти два направления работой, либо убираем их со страницы. Заявлять направление
без работы за ним — ровно то, за что сайт и критикуют.

## 5. Гигиена: единый чек-лист на каждый витринный репозиторий

Сейчас у всех двадцати пяти: 0 topics, 0 homepage, ни одной лицензии.

- [ ] `description` — есть у всех, проверить формулировки
- [ ] `homepage` → адрес демо на `lendvis.ru`
- [ ] `topics` — 4–6 штук, по направлению и стеку
- [ ] `LICENSE` — MIT. **Гейт:** у работ с командным авторством (`allTogether`/AutoCheck,
      `screenplay-to-shotlist`, `volgaIT2025-flutter`) лицензию ставим только после согласия
      соавторов — MIT на чужой вклад без спроса ставить нельзя. Атрибуция команды в README
- [ ] README: RU + EN-резюме, кадры в галерее, логотип-эталон, ссылка на хаб
- [ ] Логотип единый: `favicon.svg` из хаба, без вариаций

Отдельно:

- [ ] **APK-релизы четырёх мобилок.** Сейчас 0 релизов — витрина мобильной разработки, где нечего запустить. Собрать release, приложить в GitHub Releases, ссылку в README и на страницу проекта
- [ ] **Pages со скринами для мобилок** — чтобы у каждой работы был адрес на `lendvis.ru`
- [ ] **Восемь работ с живыми демо не показаны на хабе.** Сверено с боевым бандлом
      `assets/index-E3Imkt6y.js`: в нём есть только восемь карточек. Отсутствуют четыре мобилки
      (опубликованы 2 сентября в 21:09–21:12, последний деплой хаба — 17:15, то есть хаб
      просто не пересобирали после них), а также `python-faq-bot`, `alice-301-backend`,
      `blockchain-practice`, `vps-setup-script` — у всех четырёх Pages живые и отдают 200
- [ ] Профиль организации: README, аватар с логотипом, pinned из флагманов
- [ ] `open-webui` (чужой форк), `kmp-presentation`, `FlipCoin` — оформить или убрать из публичных

## 6. Порядок работ и гейты

**Не блокируется ничем — делаем сейчас:**

1. Контент страниц проектов: тексты, кадры, рамки устройств для всех тринадцати работ
2. Шесть NDA-кейсов в формат страниц
3. Перестройка витрины хаба: маршруты, страница проекта, фильтр по направлениям, снятие HashRouter
4. Сборка APK четырёх мобилок и подготовка релизов
5. Оформление двух недостающих направлений (модель СЦ, методика аудита)
6. Заготовки: `CNAME`, nginx-конфиги поддоменов, systemd-юниты для drawer и quiz, deploy-скрипт на VPS, список DNS-записей — файлами, готовыми к применению

**Требует явного «поехали» от владельца (ломает живые ссылки):**

7. Создание организации `lendvis` и перенос тринадцати витринных репозиториев. `FlipCoin` остаётся на личном аккаунте по старому адресу — иначе ломается живой жетон

**Заблокировано до одобрения eSIM:**

8. Покупка `lendvis.ru` и VPS в Timeweb, регистрация почт
9. DNS: апекс на Pages, поддомены на VPS, MX
10. Разворот drawer и quiz на VPS, TLS
11. Публикация и анонс

**Руками владельца, вне очереди:**

12. Ротация ключей: OpenAI, Gemini ×2, Tuya, Gitea, токен бота, пароли dem-exam-стенда, ЮKassa/МойСклад/RetailCRM. Репозитории приватные, но всё, что хоть день лежало в публичном, считается скомпрометированным навсегда

## 7. Вне объёма этой спеки

- Хвосты потоков: блокчейн 2–8 (multichain-kit, waves ×2, fabric ×2, eth-guide) и мобилки 2/4/5/7 (pgkfood-kmp, autocheck-flutter, compose-language-app, multimodule-showcase). Идут следующим циклом, витрина от них не зависит. Пункт «geo-album» из плана мобилок сюда не попадает — это чистая гигиена репозитория, закрывается чек-листом раздела 5
- `pgk-champs` остаётся отдельным продуктом в своей организации
- PGK Food публикуется только кейсом; код клиентский
- EN-версия сайта уже есть и поддерживается наравне с RU, отдельной задачей не выделяется
